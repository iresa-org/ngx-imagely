import { ImagelyDirective } from './ngx-imagely.directive';

describe('ImagelyDirective', () => {
  it('should create an instance', () => {
    // const directive = new ImagelyDirective();
    // expect(directive).toBeTruthy();
  });
});
